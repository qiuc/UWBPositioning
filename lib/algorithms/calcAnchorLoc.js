/**
 * Created by chenqiu on 15/8/21.
 */

module.exports = function(anchors, mirror, callback) {
    // First anchor will be positioned to [0,0]
    anchors[0].position = [0, 0];

    // Second anchor will in the same y position as first one

};